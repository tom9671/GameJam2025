using UnityEngine;

public class Canvas_MainMenu : MonoBehaviour
{
    void Awake()
    {
        Screen.SetResolution(1440, 801, true);
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
